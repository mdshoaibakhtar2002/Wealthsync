import { Stack, Typography } from '@mui/material';

const Settings = () =>{
    return(
        <Stack width={'100%'} height={'100%'} display={'flex'} justifyContent={'center'} position={'absolute'}>
            <Typography color={'gray'}>Loading...</Typography>
        </Stack>
    )
}

export default Settings